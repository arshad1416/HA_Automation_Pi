# ESPHome secrets template — copy to `secrets.yaml` (same directory) and fill in.
# NEVER commit the real secrets.yaml (it is gitignored).
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
