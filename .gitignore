# === Secrets & Auth (NEVER commit) ===
secrets.yaml
.storage/auth
.storage/auth_provider.*
.storage/http.auth
.storage/onboarding
.storage/core.config
# Thread operational dataset TLV embeds the mesh network key
.storage/thread.datasets

# === Databases ===
*.db
*.db-shm
*.db-wal

# === Logs ===
*.log
*.log.*
*.fault

# === Cache / Temp / Generated ===
.cache/
.cloud/
deps/
tts/
.ha_run.lock

# === Python ===
__pycache__/
*.pyc

# === OS ===
.DS_Store
Thumbs.db
.storage/core.uuid
.storage/roborock/
.storage/core.config_entries
.storage/google.*
backups/
tmp_backups/
*.tar
.storage/application_credentials
.storage/backup
.storage/mobile_app
.storage/core.restore_state
.storage/*.pickle
.storage/*.pem
.storage/*.key
.storage/esphome.*
.storage/sonoff/
.storage/alexa_media.*
.storage/core.labs
.storage/core.config_entries*
.storage/core.config_entries.bak.*

# === Editor backup copies (git history is the backup; don't track snapshots) ===
*.bak
*.bak.*
*.bak-*
*.pre-*-fix

# === Volatile machine state (rewritten by HA/daemons on the Pi; tracking these
# drowned curated changes in history — .storage/ registries stay tracked for recovery) ===
.storage/smart_irrigation.storage*
.storage/trace.saved_traces
.storage/repairs.issue_registry
.storage/core.analytics
.storage/bluetooth.*
.storage/nest.event_media
.storage/homekit_controller-entity-map
.storage/hacs.data
.storage/hacs.repositories
.storage/http
tuya_discovery_log.txt
tuya_discovery_devices.json
grizzl_e_state.json
grizzl_e_history.json
apsystems.json

# === ESPHome (curated configs live in esphome/; the LIVE configs with real WiFi
# credentials stay in /opt/esphome on the Pi and are NEVER committed) ===
esphome/secrets.yaml
esphome/.esphome/
esphome/**/.pioenvs/
*.pioenvs/

# Pre-change backup copies of .storage registries. These carry live integration
# credentials (config_entries) and were never meant to be tracked — GitHub push
# protection blocked 134 commits on them (2026-08-07 .. 2026-08-08).
# CLAUDE.md: do not create *.bak copies — git history is the backup.
.storage_bak/
